{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "build": "tsup src/index.ts",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@repo/common": "*",
    "@types/express": "^5.0.2",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "esbuild": "0.25.4",
    "tsup": "^8.5.0"
  }
}
